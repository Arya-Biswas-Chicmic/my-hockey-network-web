# Server-only backend origin used by the Next.js same-origin BFF.
# Copy to .env.local. Never commit the runtime file.
API_BASE_URL=https://development-api.example.com/v1

# Public canonical web origin used for metadata. It contains no secret.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
