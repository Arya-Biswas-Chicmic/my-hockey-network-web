/**
 * Compatibility export for screens that still import the historical common path.
 * The desktop sidebar has one implementation under the layout layer.
 */
export { LeftSidebar as Sidebar } from '@/components/layout/LeftSidebar';
export type { LeftSidebarProps as SidebarProps } from '@/components/layout/LeftSidebar';
